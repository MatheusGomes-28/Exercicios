const primeiroNome = "João"; //A variável não pode ter "_".
const dataNascimento = "01/01/2000";//O padrão é que se inicie com letra minúscula.
const salario = 3000;//Sem mais que uma palavra, as variáveis devem ser apenas letras minúsculas.
let numeroDeTentativas = 0;//O camelCase usa letras maiúsculas e minúsculas, é ideial separar a cada começo de palavra.
const nomeCompleto = "João Silva";//O padrão é que se inicie com letra minúscula.